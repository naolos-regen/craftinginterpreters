Just-in-time compilation tends to be the fastest way to implemnt dynamically<br>
typed languages, but not all of them use it. What reasons are there to *not* JIT?<br>

If for instance my first language I programmed is an interpreted programming language that is also dynamically typed, the burden is that I would also need to write independend CPU architecture code that translates my "interpreter" or compiler into an intermediate representation (byte-code). It's definately not unachieveable, but enthusiastic to say the least.<br>

Another example, could be programming an Operating System without having much knowledge in the field. It quickly humbles us and it tends to still be unforgiveable.
